package com.example.vinyl.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Palette sampled from the Figma hi-fi design system and the exported screens.
 */
object VinylColors {
    // Page background used by every exported screen.
    val Ink = Color(0xFF0D0D0D)
    val Charcoal = Color(0xFF1E1E1E)
    val Surface = Color(0xFF242A2A)
    val Cream = Color(0xFFF4F0EA)
    val Teal = Color(0xFF77EDE5)
    val Rust = Color(0xFF6B3B32)

    // Unselected filter pill.
    val Pill = Color(0xFF303030)
    // Shelf ledge: lit top face fading into the shadowed front edge.
    val ShelfTop = Color(0xFF7A4439)
    val ShelfFade = Color(0xFF442924)
    val ShelfEdge = Color(0xFF221717)
    // Thin highlight around a record sleeve.
    val SleeveBorder = Color(0xFF716F6D)
    val NavBar = Color(0xFF0F0F0F)
    val Divider = Color(0xFF484746)
}
