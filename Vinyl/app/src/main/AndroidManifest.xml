<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Sprint 2 (GPS): coarse is what the feature actually needs, since we only ever keep the
         city centroid. Fine is requested alongside it so the system dialog offers the precise
         option; the app works fine if the user grants approximate only. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Declared explicitly as optional: a location permission otherwise implies a required
         hardware feature, which would hide the app from devices without GPS. -->
    <uses-feature
        android:name="android.hardware.location"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vinyl">
        <!-- singleTask: the OAuth callback below must be delivered to the *running* activity via
             onNewIntent. Without it Android starts a second MainActivity and the Compose state,
             including the auth gate, restarts from scratch. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Vinyl"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Return leg of the browser sign-in fallback, used when the device has no Google
                 account for Credential Manager to offer. Must match scheme/host in SupabaseClient. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="auth-callback"
                    android:scheme="com.example.vinyl" />
            </intent-filter>
        </activity>
    </application>

</manifest>